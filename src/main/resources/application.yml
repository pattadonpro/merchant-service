server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/

spring:
  application:
    name: merchant
  datasource:
    url: jdbc:mysql://localhost:3306/merchant
    username: root
    password:
